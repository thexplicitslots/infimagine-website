create table if not exists public.quote_requests (
  id bigint generated by default as identity primary key,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  customer_name text,
  contact text,
  project_type text,
  quantity text,
  size text,
  dimensions text,
  readiness text,
  reference_link text,
  attachments jsonb default '[]'::jsonb,
  description text,
  ai_possibilities text,
  material text,
  color text,
  finish text,
  strength text,
  timeline text,
  budget text,
  delivery text,
  location text,
  estimate text,
  status text default 'New',
  priority text default 'Normal',
  slice_status text,
  estimated_print_time_minutes numeric,
  estimated_filament_grams numeric,
  estimated_filament_cost numeric,
  slicer_profile text,
  slicer_error text,
  sliced_at timestamptz,
  follow_up_date text,
  notes text,
  source text
);

alter table public.quote_requests
  add column if not exists created_at timestamptz default now(),
  add column if not exists updated_at timestamptz default now(),
  add column if not exists customer_name text,
  add column if not exists contact text,
  add column if not exists project_type text,
  add column if not exists quantity text,
  add column if not exists size text,
  add column if not exists dimensions text,
  add column if not exists readiness text,
  add column if not exists reference_link text,
  add column if not exists attachments jsonb default '[]'::jsonb,
  add column if not exists description text,
  add column if not exists ai_possibilities text,
  add column if not exists material text,
  add column if not exists color text,
  add column if not exists finish text,
  add column if not exists strength text,
  add column if not exists timeline text,
  add column if not exists budget text,
  add column if not exists delivery text,
  add column if not exists location text,
  add column if not exists estimate text,
  add column if not exists status text default 'New',
  add column if not exists priority text default 'Normal',
  add column if not exists slice_status text,
  add column if not exists estimated_print_time_minutes numeric,
  add column if not exists estimated_filament_grams numeric,
  add column if not exists estimated_filament_cost numeric,
  add column if not exists slicer_profile text,
  add column if not exists slicer_error text,
  add column if not exists sliced_at timestamptz,
  add column if not exists follow_up_date text,
  add column if not exists notes text,
  add column if not exists source text;
